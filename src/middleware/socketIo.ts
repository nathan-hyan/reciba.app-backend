import iosocket, { Socket } from "socket.io";

/**
 * Funcion para habilitar la conección WebSocket
 * @param {*} server Servidor HTTP
 */

function iosConnect(server: any) {
  
}

export default iosConnect;
